import {makeStyles, createStyles} from '@material-ui/core';

export const useStyles = makeStyles((theme) => createStyles({
  list: {
    margin: '0 20%',
    backgroundColor: '#42adf5 !important',
    maxHeight: 500,
    overflow: 'auto'    
  } 
    
  }));